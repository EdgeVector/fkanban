node_modules/
*.log
.DS_Store
.env
.env.*

# Vendored @lastdb/app-sdk ships as prebuilt dist/ for now. Keep that nested
# dist tracked even if a broader dist ignore is added later.
!/vendor/lastdb-app-sdk/dist/

# Local fold_db node data dir — created when a CLI/test run points init at a
# node rooted in the checkout (sled db + blobs + snapshots). Never committed.
/data/
